# By creating the __init__.py, under the models folder we convert it to a package.
# which will help us import our models easily.
from models.store import StoreModel
from models.item import ItemModel
